<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context=".MainActivity">
    <Spinner
        android:id="@+id/spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/spinner_left_margin"></Spinner>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <GridView
        android:id="@+id/grid_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:numColumns="auto_fit"
        android:columnWidth="@dimen/grid_view_column_width"
        android:verticalSpacing="@dimen/grid_view_vertical_space"
        android:horizontalSpacing="@dimen/grid_view_horizontal_space"
        android:stretchMode="spacingWidthUniform">
    </GridView>

     <TextView
         android:id="@+id/error_text"
         android:layout_width="match_parent"
         android:layout_height="match_parent"
         android:textSize="12sp"
         android:text="Network Error"
         android:textStyle="bold"
         android:paddingTop="@dimen/detail_activity_element_top"
         android:paddingLeft="@dimen/detail_activity_element_left"
         android:paddingRight="@dimen/detail_activity_element_right"
         android:gravity="center_horizontal"
         android:visibility="invisible"/>
    </FrameLayout>

</LinearLayout>
